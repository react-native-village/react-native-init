import React from 'react'
import { StyleSheet, View } from 'react-native'
import { oneSelectT } from '../../../types/LessonTypes'

export function SelectOne({}: oneSelectT) {
  return <View></View>
}

const styles = StyleSheet.create({})
