import React from 'react'
import { StyleSheet, View } from 'react-native'
import { supplementT } from '../../../types/LessonTypes'

export function SupplementText({}: supplementT) {
  return <View></View>
}

const styles = StyleSheet.create({})
